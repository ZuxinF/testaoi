"""Building function segmentation pipeline.

The package keeps data preparation, model construction, training, and
polygonization separate so the model can be swapped without changing the GIS
data flow.
"""

